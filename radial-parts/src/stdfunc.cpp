#include <stdfunc.h>

//Handle one line of input and return it
std::string handleinput()
{
    //Temp variable
    std::string ret;
    std::cin >> ret;
    return ret;
}

//Print a message with a return carriage!
void printl(std::string msg)
{
    std::cout << msg << std::endl;
}
